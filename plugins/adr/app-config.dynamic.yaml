dynamicPlugins:
  frontend:
    backstage.plugin-adr:
      mountPoints:
        - mountPoint: entity.page.overview/cards
          importName: EntityAdrContent
          config:
            layout:
              gridColumn: "1 / -1"
